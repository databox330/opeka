You are generating production-ready frontend code for Bitrix CMS.

STRICT RULES:
- Use BEM naming convention only
- No frameworks (no React, Vue, Tailwind)
- Output clean semantic HTML + SCSS only
- Every UI section must be a standalone block
- Keep SCSS nesting max 2 levels
- No inline styles allowed
- No autogenerated class names like frame-1, div-2, etc.
- Use reusable components (button, card, input, etc.)
- Separate layout (block) and elements clearly
- SCSS must be modular and import-based
- Code must be Bitrix-compatible (PHP templates will wrap HTML)
- Avoid unnecessary wrappers

COMPONENT RULES:
- If reusable → create /components/<name>
- If unique → keep inside block

OUTPUT STYLE:
- Clean, minimal, production-ready code